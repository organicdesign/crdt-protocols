{
  "name": "crdt-protocols",
  "version": "1.0.0",
  "description": "Protocols for encoding and decoding CRDT data.",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    "./*": {
      "import": "./dist/*/index.js",
      "types": "./dist/*/index.d.ts"
    }
  },
  "scripts": {
    "build": "protons ./protobufs/*.proto && mkdir -p dist build-ts && touch dist/cleanup build-ts/cleanup && rm -R dist/* build-ts/* && ./move-protos.sh && tsc -b && rm -R build-ts"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@code.organicdesign.nz:222/organicdesign/crdt-protocols.git"
  },
  "author": "Saul Boyd",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@swc/core": "^1.3.22",
    "protons": "^6.0.1",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "protons-runtime": "^4.0.1"
  }
}
