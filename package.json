{
  "name": "crdt-protocols",
  "version": "1.0.0",
  "description": "Protocols for encoding and decoding CRDT data.",
  "main": "dist/index.js",
  "scripts": {
    "build": "protons ./protobufs/*.proto && mv ./protobufs/*.ts ./src/ && mkdir -p dist && touch dist/cleanup && rm dist/* && tsc -b"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@code.organicdesign.nz:222/organicdesign/crdt-protocols.git"
  },
  "author": "Saul Boyd",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@swc/core": "^1.3.22",
    "protons": "^6.0.1",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "protons-runtime": "^4.0.1"
  }
}
